# =============================================================================
# Phase 0 placeholder Dockerfile.
# Phase 1 replaces this with a multi-stage Python 3.12 build for FastAPI.
# =============================================================================

FROM alpine:3.20

LABEL phase="0"
LABEL purpose="placeholder — Phase 1 will replace with FastAPI image"

WORKDIR /app

CMD ["sh", "-c", "echo '[tg365-api] Phase 0 placeholder. Phase 1 introduces FastAPI.'; sleep infinity"]
