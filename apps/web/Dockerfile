# =============================================================================
# Phase 0 placeholder Dockerfile.
# Phase 1 replaces this with a multi-stage Node 20 build for Next.js standalone.
# =============================================================================

FROM alpine:3.20

LABEL phase="0"
LABEL purpose="placeholder — Phase 1 will replace with Next.js image"

WORKDIR /app

CMD ["sh", "-c", "echo '[tg365-web] Phase 0 placeholder. Phase 1 introduces Next.js.'; sleep infinity"]
